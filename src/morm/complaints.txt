when table is created at first, indexes are not created, but when i save again even though the data has not been updated, then indexes are created

i created a table, const Pri = morm?.model({
  table: "pri",
  columns: [
    {
      name: "id",
      type: "int",
      primary: true,
      default: "int()",
      notNull: false,
      unique: true,
    },
  ],
  indexes: ["id", "name"],
  sanitize: true, // false
});, was successful, but on saving again when when the data were not change, then i saw error message: 
  ╔══════════════════════════╗
  ║  MORM  ·  MIGRATION    ║
  ╚══════════════════════════╝

  COLUMNS  ›  pri
  └─ Dropped      id

  ERROR  ›  pri
  └─ MIGRATION    Invalid index on "pri": column "id" does not exist

  ✗  Completed with errors  ·  1 table  ·  1 change  ·  1 error